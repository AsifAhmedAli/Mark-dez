import React from "react";
import { TouchableOpacity, View, Text } from "react-native";
import { styled } from "nativewind";
const StyledView = styled(View);
const StyledText = styled(Text);
const Logout_functionality = (props) => {
  // console.log(props);
  // console.log(props.route.params.state.authorisation);
  // props.route.params.setauthorisation_state();
  // console.log(props.route.params.state.authorisation);
  // props.navigation.navigate("Income");
  // return (
  //   <StyledView>
  //     <StyledText>asdfasdf</StyledText>
  //   </StyledView>
  // );
};
export default Logout_functionality;
